version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    environment:
      - DEBUG=false
      - DATABASE_URL=sqlite:///./mingli.db
    restart: unless-stopped